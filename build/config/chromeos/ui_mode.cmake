include(${PROJECT_SOURCE_DIR}/build/config/chromeos/args.cmake)

set(CHROMEOS_IS_BROWSER_ONLY OFF)
set(ALSO_BUILD_ASH_CHROME OFF)
set(ALSO_BUILD_LACROS_CHROME OFF)
set(ALSO_BUILD_LACROS_CHROME_FOR_ARCHITECTURE "")

if(CHROMEOS AND NOT CHROMEOS_IS_BROWSER_ONLY)
  set(CHROMEOS_ASH ON)
else()
  set(CHROMEOS_ASH OFF)
endif()

if(CHROMEOS AND CHROMEOS_IS_BROWSER_ONLY)
  set(CHROMEOS_LACROS ON)
else()
  set(CHROMEOS_LACROS OFF)
endif()
